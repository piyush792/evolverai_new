import { NodespipesPipe } from './nodespipes.pipe';

describe('NodespipesPipe', () => {
  it('create an instance', () => {
    const pipe = new NodespipesPipe();
    expect(pipe).toBeTruthy();
  });
});
